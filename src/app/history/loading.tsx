import { PageLoading } from "@/components/ui/PageLoading";

export default function Loading() {
  return (
    <PageLoading
      title="History"
      subtitle="Preparing local activity and explorer-link rows."
    />
  );
}
